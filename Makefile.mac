CC ?= clang
CFLAGS ?= -O2 -std=c11 -Wall -Wextra -D_DARWIN_C_SOURCE
LDFLAGS ?=
SELFHOST_LIBS ?= -pthread -lm
EXEEXT ?=

include mk/selfhost.mk

# macOS > 15: stage-0 is compiled from C, then the active frontend and
# converter are compiled twice from CMotive source using clang by default.
